<nav class="navbar navbar-expand-lg">
  <div class="container-fluid custom-navbar-container">
    <%= link_to "Alpha Blog", root_path, class: "navbar-brand nav-custom-brand alpha-blog-color", id: "logo" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarAlpha" aria-controls="navbarAlpha" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse navbar-right" id="navbarAlpha">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link", id: "home" %>
        </li>
        <li class="nav-item dropdown nav-item-custom">
          <a class="nav-link dropdown-toggle" href="#" id="articleDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Article
          </a>
          <ul class="dropdown-menu" aria-labelledby="articleDropdown">
            <li><%= link_to "Articles", articles_path, class: "dropdown-item", id: "articles" %></li>
            <% if logged_in? %>
              <li><%= link_to "Create new article", new_article_path, class: "dropdown-item", id: "new-article" %></li>            
            <% end %>
          </ul>
          <% if logged_in? %>
            <li class="nav-item">
              <%= link_to "Users", users_path, class: "nav-link", id: "users" %>
            </li>
          <% end %>
        </li>
        <li class="nav-item nav-item-custom">
          <%= link_to "About", about_path, class: "nav-link", id: "about" %>
        </li>
        <% if logged_in? %>
          <li class="nav-item dropdown nav-item-custom nav-dropdown-custom">
            <a class="nav-link dropdown-toggle nav-item-child-custom" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.username%>
            </a>
            <ul class="dropdown-menu" aria-labelledby="userDropdown">
              <li><%= link_to "Profile", current_user, class: "dropdown-item", id: "current-user" %></li>
              <li><%= link_to "Logout", logout_path, class: "dropdown-item", data:{ turbo_method: :delete}, id: "logout" %></li>            
            </ul>            
          </li>
        <% else %>
          <li class="nav-item nav-active-custom">
            <%= link_to "Log in", login_path, class: "nav-link active active-custom-reverse", id: "login" %>
          </li>
          <li class="nav-item nav-active-custom">
            <%= link_to "Sign up", signup_path, class: "nav-link active active-custom", id: "signup" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>